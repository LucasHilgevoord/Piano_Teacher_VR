namespace PianoTeacher.Piano.Keys
{
    /// <summary>
    /// accidental is a note of a pitch (or pitch class) that is not a member of the scale or mode indicated by the most recently applied key signature.
    /// https://en.wikipedia.org/wiki/Accidental_(music)
    /// </summary>
    public enum KeyAccidentals
    {
        None = 0,
        Natural = 1,
        Sharp = 2,
        Flat = 3
    }
}
